guide <- tabPanel(title = list(icon("database"), "GUIDE"), 
                               value = "guide",
                               br(), br(),
                               uiOutput("ui_guide")
)